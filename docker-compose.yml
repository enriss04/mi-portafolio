services:
  next-portafolio:
    build:
      context: .
      dockerfile: dockerfile
    container_name: portafolio-enrique
    ports:
      - "7000:3000"
    restart: always
    networks:
      - net-tecnomex

networks:
  net-tecnomex:
    external: true